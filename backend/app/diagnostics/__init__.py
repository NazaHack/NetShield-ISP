"""Operator-facing diagnostic entry points.

These modules are run by hand or by deployment smoke tests. They are kept out of
the HTTP API because they report on infrastructure rather than on tenant data.
"""
