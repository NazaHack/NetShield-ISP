# =============================================================================
#  NetShield-ISP — backend runtime dependencies
#  Compatible-release pins (~=) keep patch-level security fixes flowing while
#  freezing the minor version that the code is tested against.
# =============================================================================

# --- Web framework -----------------------------------------------------------
fastapi~=0.115.0
uvicorn[standard]~=0.34.0
python-multipart~=0.0.20

# --- Validation & settings ---------------------------------------------------
pydantic~=2.10.0
pydantic-settings~=2.7.0
email-validator~=2.2.0

# --- Persistence -------------------------------------------------------------
SQLAlchemy[asyncio]~=2.0.36
alembic~=1.14.0
asyncpg~=0.30.0
psycopg[binary]~=3.2.0

# --- Task queue --------------------------------------------------------------
celery[redis]~=5.4.0
redis~=5.2.0

# --- Security ----------------------------------------------------------------
PyJWT~=2.10.0
argon2-cffi~=23.1.0
# Nmap XML is parsed with defusedxml rather than the standard library: the
# document embeds banner text controlled by the scanned host, and the stdlib
# parsers are vulnerable to entity-expansion denial of service.
defusedxml~=0.7.1

# --- Observability -----------------------------------------------------------
structlog~=24.4.0
orjson~=3.10.0

# --- Utilities ---------------------------------------------------------------
httpx~=0.28.0
tenacity~=9.0.0
